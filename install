#!/usr/bin/env bash

here=$(readlink -f "$0")
here=$(dirname "$here")
target="$here/.git/hooks/pre-commit"

if [ -L "$target" ]; then
	printf "Pre-commit hooks are already installed at:\n    %s" % "$target"
else
	printf "Installing pre-commit hooks at:\n    %s" % "$target"
	source="$here/pre-commit.sh"
	ln -s "$source" "$target"
fi
