name: ci

on:
  pull_request:
    branches:
      - master

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    steps:
      - uses: actions/checkout@v2
      - run: |
          ./.pre-commit-hooks/install
          ./.git/hooks/pre-commit --all
